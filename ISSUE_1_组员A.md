# [组员A] 首页完善：搜索框 + 筛选 + 商品卡片 + 校区切换

## 任务分配：组员A

### 负责文件
- `templates/index.html`（主要）
- `data/site_info.py`（辅助）

### 功能要求
1. **搜索框**：输入关键词实时过滤商品（前端 JS 实现）
2. **筛选条**：按「分类」「校区」下拉筛选
3. **商品卡片**：展示图片、标题、价格、发布者、时间
4. **校区切换**：顶部 Tab 切换不同校区数据

### 数据来源
- `listings.get_listings()` 获取商品列表
- `categories.get_campus_list()` 获取校区列表
- `categories.get_category_list()` 获取分类列表

### 验收标准
- [ ] 搜索框能实时过滤
- [ ] 筛选下拉能正常工作
- [ ] 商品卡片样式美观
- [ ] 切换校区后列表更新

### 完成后
提交 PR 到 `master` 分支，@litinfyao 审核。
